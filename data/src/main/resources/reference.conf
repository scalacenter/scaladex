app {
 env = "local"
}
database {
  user = "user"
  user = ${?POSTGRESQL_USERNAME}
  password = "password"
  password = ${?POSTGRESQL_PASSWORD}
  table = "scaladex"
  table = ${?POSTGRESQL_DATABASE}
  database-url = "jdbc:postgresql://"${database.user}":"${database.password}"@localhost:5432/"${database.table}
}
data-paths {
  contrib = "scaladex-contrib"
  index = "scaladex-small-index"
  index = "/Users/meriamlachkar/scaladex-data/scaladex-index"
  credentials = "scaladex-credentials"
}